import { StyleSheet } from 'react-native';

const styled = {
    container: {
        flex: 1,
        alignItems: "center",
        marginTop: 10,
    },
};

const styles = StyleSheet.create(styled);
export default styles;